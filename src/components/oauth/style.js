import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  root: {
    marginTop: 30
  },
  button: {
    alignItems: "center",
    backgroundColor: "#DDDDDD",
    borderRadius: 5,
    padding: 3,
  },
  tinyLogo: {
    width: 40,
    height: 40,
  },
})